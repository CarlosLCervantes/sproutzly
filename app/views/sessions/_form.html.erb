<%= form_tag(sessions_path, :method => :post, :html => { :class => 'form-horizontal' }) do |f| %>
  <!--<%# if @session.errors.any? %>
    <div id="error_explanation">
      <h2><%#= pluralize(@session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <%# @session.errors.full_messages.each do |msg| %>
        <li><%#= msg %></li>
      <%# end %>
      </ul>
    </div>
  <%# end %>-->
  <div class="control-group">
    <%= label_tag :email, "Email", :class => 'control-label' %>
    <div class="controls">
      <%= text_field_tag :email, "", :class => 'text_field', :placeholder => "Sample@website.com" %>
    </div>
  </div>

  <div class="control-group">
    <%= label_tag :password, "Password", :class => 'control-label' %>
    <div class="controls">
      <%= password_field_tag :password, "", :class => 'text_field' %>
    </div>
  </div>


  <div class="actions">
    <%= submit_tag "Login", :class => "btn btn-primary btn-large" %>
  </div>
<% end %>
